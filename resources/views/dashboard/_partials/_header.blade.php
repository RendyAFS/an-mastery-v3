<div class="flex flex-wrap justify-between items-center">
    <div class="mb-3 md:mb-0">
        <h1 class="text-3xl font-bold">{{ __('dashboard.title') }}</h1>
        <p class="text-sm text-(--color-dark-gray) mt-1">{{ __('dashboard.description') }}</p>
    </div>

    <div class="text-right">
        <p id="dashboard-clock-date" class="text-sm font-medium text-(--color-dark) dark:text-(--color-light)"></p>
        <p id="dashboard-clock-time" class="text-2xl font-bold text-(--color-primary)"></p>
    </div>
</div>
